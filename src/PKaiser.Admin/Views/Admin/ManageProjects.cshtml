@model IEnumerable<Project>
@{
    this.ViewData["Title"] = "Manage Projects";
}

<component type="typeof(HeaderComponent)"
           param-Title="@this.ViewData["Title"]"
           render-mode="Static"/>

<div class="container my-3">
    <div class="row">
        <div class="col-12 text-center p-3">
            <a class="btn btn-primary btn-outline" asp-controller="Project"
               asp-action="AddProject">

               + Add Project
           </a>
        </div>
    </div>

    <div class="row">
        <div class="col-12">
            <table class="table text-white w-100 mt-3">
                <thead>
                    <tr>
                        <th scope="col">
                            Id
                        </th>
                        <th scope="col">
                            Title
                        </th>
                        <th scope="col">
                            Actions
                        </th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (Project project in this.Model)
                    {
                        <tr>
                            <th scope="row">
                                @project.Id
                            </th>
                            <td>
                                @project.Title
                            </td>
                            <td>
                                <a class="text-white" asp-controller="Project"
                                   asp-action="ViewProject" asp-route-id="@project.Id">

                                    View
                                </a>
                                |
                                <a class="text-white" asp-controller="Project"
                                   asp-action="EditProject" asp-route-id="@project.Id">

                                    Edit
                                </a>
                                |
                                <a class="text-white" asp-controller="Project"
                                   asp-action="DeleteProject" asp-route-id="@project.Id">

                                    Delete
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>