@model IEnumerable<Blog>
@{
    this.ViewData["Title"] = "Manage Blogs";
}

<component type="typeof(HeaderComponent)"
           param-Title="@this.ViewData["Title"]"
           render-mode="Static"/>

<div class="container">
    <div class="row">
        <div class="col-12 text-center p-3">
            <a class="btn btn-outline-light btn-lg" asp-area=""
                asp-controller="Blog" asp-action="Post">

                + Post Blog
            </a>
        </div>
    </div>

    <div class="row">
        <div class="col-12">
            <table class="table">
                <thead>
                    <tr>
                        <th scope="col">
                            Id
                        </th>
                        <th scope="col">
                            Title
                        </th>
                        <th scope="col">
                            Actions
                        </th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (Blog blog in this.Model)
                    {
                        <tr>
                            <th scope="row">
                                @blog.Id
                            </th>
                            <td>
                                @blog.Title - @blog.DatePosted.ToString("MMMM-dd-yyyy")
                            </td>
                            <td>
                                <a asp-controller="Blog" asp-action="ViewProject">
                                    View
                                </a>
                                |
                                <a asp-controller="Blog" asp-action="">
                                    Edit
                                </a>
                                |
                                <a>
                                    Delete
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>